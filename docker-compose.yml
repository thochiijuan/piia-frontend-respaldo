services:
  frontend:
    build:
      context: ./react-javascript/
      dockerfile: Dockerfile
    environment:
      - APP_HOST=${APP_HOST}
    expose:
      - "5173"
    networks:
      - piia-platform

networks:
  piia-platform:
    external: true
