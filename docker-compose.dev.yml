services:
  frontend:
    volumes:
      - ./react-javascript/develop/:/app
    # command: npm run dev
    command: sh -c "npm install --verbose && tail -f /dev/null"
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
